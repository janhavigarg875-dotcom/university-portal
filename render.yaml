services:
  - type: web
    name: eduglobal-university-portal
    runtime: node
    plan: free
    buildCommand: "echo 'no build step needed'"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_KEY
        sync: false
    autoDeploy: true
