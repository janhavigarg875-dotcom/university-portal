{
  "name": "university-portal",
  "version": "1.0.0",
  "description": "EduGlobal University portal — full-stack (Node.js backend + static frontend), no dependencies required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=16"
  }
}
